# SudokuValidation
Checks whether a given Sudoku is valid or not in Rust\
Uses a bitmap (three instances of it) with only 86bit for variables in it (so 258bit in total)\
to scan the board which uses a bitmap of 324bit\
\
Made for learning bitmaps
